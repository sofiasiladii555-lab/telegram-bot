app = "duty-bot-willow-tree-9"
primary_region = "fra"

[build]

[env]
  PYTHONUNBUFFERED = "1"

[mounts]
  source = "bot_data"
  destination = "/data"

[[vm]]
  memory = "256mb"
  cpu_kind = "shared"
  cpus = 1
